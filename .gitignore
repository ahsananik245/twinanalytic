# Dependency directories
node_modules/

# Local env files
.env.local
.env.*.local

# Vercel directory
.vercel/

# Editor directories and files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS metadata files
.DS_Store
Thumbs.db

# Agent skills (tooling, not site content — must not be published to the web).
# skills-lock.json is kept so the installed skill set stays reproducible:
#   npx skills add <repo>
.agents/
.claude/skills/
